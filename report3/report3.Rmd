---
title: |
 | Universidade Federal do Amazonas 
 | Bacharelado em Matemática
subtitle: | 
    | Laboratório de Física I
    | Relatório III
author: | 
    | Gabriel Bezerra de M. Armelin - 21550325
    | Mario Alves Pardo Junior - 21553964
    | Jonas Miranda Cascais Júnior - 21553844
    | Fabrício Yuri Costa da Silva - 21454545
framework: io2012
output: pdf_document
highlighter: highlight.js
hitheme: tomorrow
job: INDT
lib: ../../librariesNew
logo: bloomberg_shield.png
mode: selfcontained
header-includes: 
    - \usepackage{amsmath}
    - \usepackage{bbm}
    - \usepackage{commath}
assets: ../../assets
url: null
widgets: mathjax
---

```{r setup, include=FALSE}
suppressWarnings(library("knitr"))
suppressWarnings(library("ggplot2"))
suppressWarnings(library("grid"))
suppressWarnings(library("gridExtra"))
opts_chunk$set(dev = 'pdf')
set.seed(12432) 
options(digits = 10) 
library(printr)
```

Introdução
===================

Este relatório descreve e analisa o experimento realizado em sala de aula na disciplina *Laboratório de Física I* do curso de Bacharelado em Matemática.


Parte Experimental
====================

O experimento consiste em estimar o valor da aceleração da gravidade no local de realizazação do experimento. Para isto, as seguintes etapas foram realizadas por 4 alunos: 

1) Coleta do tempo de queda de uma esfera em diversas alturas diferentes. Para a medição deste tempo, foi utilizado um aparelho apropriado para a tarefa. Este aparelho é capaz de medir o tempo de queda com precisão de 0.001 segundos.
2) Estimatica da aceleração da gravidade
3) Estimativa do erro

Tratamento de Dados
====================

Esta seção apresenta os dados e cálculos em cada atividade descrita na seção *Parte Experimental*.

## 1) Coleta das amostras
A primeira atividade diz respeito à coleta das amostras de tempo de queda da espera em diversas distâncias e por 4 alunos diferentes. A próxima tabela apresenta os valores coletados:

```{r echo=F}
dfTempoQueda <- data.frame("0.10m"=c(0.1371, 0.1381, 0.1362, 0.1357), "0.15m"=c(0.1701, 0.1718, 0.1703, 0.1716), "0.20m"=c(0.1963, 0.1997, 0.1980, 0.1972), "0.25m"=c(0.2213, 0.2241, 0.2222, 0.2223), "0.30m"=c(0.2443, 0.2460, 0.2448, 0.2434), "0.35m"=c(0.2653, 0.2656, 0.2650, 0.2657))
dfTempoQueda <- rbind(dfTempoQueda, colMeans(dfTempoQueda))
rownames(dfTempoQueda) <- c("Aluno1", "Aluno2", "Aluno3", "Aluno4", "Média")
knitr::kable(dfTempoQueda, digits = 5, col.names=c("0.10 m (s)", "0.15 m (s)", "0.20 m (s)", "0.25 m (s)", "0.30 m (s)", "0.35 m (s)"), caption = 'Amostras dos tempos de queda da esfera em segundos.')
```

## 2) Estimativa da aceleração da gravidade

Esta seção descreve o processo utilizado para estimar a aceleração da gravidade. O primeiro será apresentar graficamente a relação de tempo de queda médio da esfera (calculado na seção anterior) com a distância percorrida. O próxmo gráfico apresenta o resultado obtido:

```{r echo=F,  fig.height=2, fig.width=4, fig.align="center"}
dist <- c(0.10, 0.15, 0.20, 0.25, 0.30, 0.35)
dfRelTempoQuedaDist <- data.frame("tempoQueda" = t(dfTempoQueda[5,]), dist = dist)
colnames(dfRelTempoQuedaDist) <- c("TempoQueda", "Dist")
ggplot(data=dfRelTempoQuedaDist, aes(x=TempoQueda, y=Dist)) +
       geom_point() +
       expand_limits(y=0) +
       xlab("Tempo de Queda (s)") + ylab("Distância (m)") +
       ggtitle("Relação tempo de queda x distância")
```

O gráfico se assemelha a uma reta ou uma leve curva. Esperava-se que a curva fosse mais acentuada já que o gráfico da relação distância-tempo em um movimento uniformemente variado é uma meia parábola. Entendemos que este gráfico se apresentou desta maneira devido ao número pequeno de amostras coletadas.

Pode calcular a aceleração utilizando a seguinte fórmula:

\begin{align}\label{eq:leiHooke}
s = s_0 + v_0t + \frac{1}{2}at^2
\end{align}

Onde:

s = corresponde a posição final. Os valores 5cm, 10cm, 15cm, 20cm, 25cm, 30cm e 35cm foram utilizados como posição final.

$s_0$ = corresponde a posição inicial. Neste caso será considerado 0 a posição inicial.

$v_0$ = corresponde a velocidade inicial. Como a esfera está em repouso em t=0, a velocidade inicial é 0.

t = corresponde ao tempo final de queda, Neste caso serão utilizados os valores dos tempos médios para cada distância percorrida que foram apresentados na tabela 1.

A seguinte tabela apresenta a aceleração da gravidade para cada distância percorrida:

```{r echo=F}
dfAceleracao <- (dist * 2) / dfTempoQueda[5,]^2
rownames(dfAceleracao) <- c("Aceleração")
knitr::kable(dfAceleracao, digits = 5, col.names=c("0.10 m (s)", "0.15 m (s)", "0.20 m (s)", "0.25 m (s)", "0.30 m (s)", "0.35 m (s)"), caption = 'Acelerações da gravidade para cada altura da esfera.')
```

A média das acelerações é:

   `r mean(as.numeric(dfAceleracao))` $m/s^2$.   

## 3) Estimativa do erro

Esta seção apresenta as estimativas dos erro de medição da seção anterior. Inicialmente, calculamos o erro de cada medição em relação a média dos tempos de queda, conforme a tabela abaixo.


```{r echo=F}
dfTempoQuedaErro <- abs(t(t(dfTempoQueda) - as.numeric(dfTempoQueda[5,]))[1:4,])
dfTempoQuedaErro <- rbind(dfTempoQuedaErro, colMeans(dfTempoQuedaErro))
rownames(dfTempoQuedaErro) <- c("Aluno1", "Aluno2", "Aluno3", "Aluno4", "Média")
knitr::kable(dfTempoQuedaErro, digits = 5, col.names=c("0.10 m (s)", "0.15 m (s)", "0.20 m (s)", "0.25 m (s)", "0.30 m (s)", "0.35 m (s)"), caption = 'Erros de medição dos tempos de queda da esfera em segundos.')
```


A partir dos erros médios dos tempos de queda, podemos estimar a variação de erro inferior da aceleração da gravidade. A tabela abaixo apresenta os resultados:


```{r echo=F}
dfAceleracaoInferior <- (dist * 2) / (dfTempoQueda[5,]+dfTempoQuedaErro[5,])^2
rownames(dfAceleracaoInferior) <- c("Aceleração")
aceleracaoInf <- mean(as.numeric(dfAceleracaoInferior))
knitr::kable(dfAceleracaoInferior, digits = 5, col.names=c("0.10 m (s)", "0.15 m (s)", "0.20 m (s)", "0.25 m (s)", "0.30 m (s)", "0.35 m (s)"), caption = 'Intervalo inferior da estimativa das acelerações da gravidade para cada altura da esfera.')
```

A média das acelerações é:

   `r aceleracaoInf` $m/s^2$.  

Por fim, estimamos a variação de erro superior da aceleracao da gravidade. A próxima tabela apresenta os resultados obtidos.

```{r echo=F}
dfAceleracaoSuperior <- (dist * 2) / (dfTempoQueda[5,]-dfTempoQuedaErro[5,])^2
rownames(dfAceleracaoSuperior) <- c("Aceleração")
aceleracaoSup <- mean(as.numeric(dfAceleracaoSuperior))
knitr::kable(dfAceleracaoSuperior, digits = 5, col.names=c("0.10 m (s)", "0.15 m (s)", "0.20 m (s)", "0.25 m (s)", "0.30 m (s)", "0.35 m (s)"), caption = 'Intervalo superior das estimativas das acelerações da gravidade para cada altura da esfera.')
```

A média das acelerações é:

   `r aceleracaoSup` $m/s^2$.  


Conclusão
====================

De acordo com os dados apresentados, pode-se concluir que a estimativa da aceleração da gravidade está no seguinte intervalo:

```{r echo=F}
dfIntervalo <- data.frame(inf=c(aceleracaoInf), sup=c(aceleracaoSup))
knitr::kable(dfIntervalo, digits = 5, col.names=c("Intervalo inferior", "Intervalo superior"), caption = 'Intervalo de estimativa da aceleração da gravidade')
```